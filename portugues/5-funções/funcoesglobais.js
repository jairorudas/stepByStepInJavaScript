/*
    Funções e Funções Globais

    Uma definição bem generica do que é uma função, poderia ser: O trecho de um codigo que se repetem em diferentes pontos,
    de nosso programa.

    Em javaScript, denominam-se funções globais as que NÃO estão associadas a um objeto particular
    da linguagem. As funções globais são.
*/

encodeURI(uri); // Codifica uma URL
decodeURI(uri); // Decodifica uma URL Codificada com encodeURI(uri)
encodeURIComponent(uri);
decodeURIComponent(uri);
escape(string);// removida da ECMAScript 3
eval(codigo);//Recebe como argumento uma string. Evitar seu uso.
isFinite(valor);
isNaN(valor);
Number(valor); // converte para numero alguma string com com um numero dentro.
parseInt(2.5);// Pode receber uma strong também com um numero dentro.
parseFloat();// Pode receber uma string também com um numero dentro.
String();// Converte para string qualquer valor passado.
